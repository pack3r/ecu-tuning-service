<!DOCTYPE html>
<html lang="pl">
  <head>
    <meta charset="UTF-8" />
    <title>Logowanie - Serwis Plików</title>
    <link rel="stylesheet" href="/tfs/styles.css" />
  </head>
  <body>
    <header class="top-bar">
      <div class="logo">Serwis Plików</div>
      <nav>
        <a href="/tfs/register" class="btn">Rejestracja</a>
        <div class="phone-number">📞 +48 533 193 112</div>
      </nav>
    </header>

    <main class="container">
      <h1>Logowanie</h1>
      <% if (error) { %>
      <div class="alert"><%= error %></div>
      <% } %>
      <form method="post" action="/tfs/login" class="card">
        <label>Email</label>
        <input type="email" name="email" required />

        <label>Hasło</label>
        <input type="password" name="password" required />

        <button type="submit" class="btn btn-primary">Zaloguj się</button>
      </form>
    </main>
  </body>
  </html>
